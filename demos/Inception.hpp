#include "Topl_App.hpp"

struct Inception_App : public Topl_App {
    Inception_App(APP_Backend b) : Topl_App(b){}

    void init() override {
        return;
    }
    void loop() override {
        return;
    }
};