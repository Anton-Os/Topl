// #define PARENT_DIR "${CMAKE_CURRENT_BINARY_DIR}/Debug/" // Change Debug directory to ${CMAKE_CFG_INTDIR}
#define ASSETS_DIR "${ASSETS_DIR}"
#define SHADERS_DIR "${SHADERS_DIR}"

#if ${Vulkan_FOUND}
    #define VULKAN_H
#endif
#if ${Rasteron_FOUND}
    #define RASTERON_ENABLE_ANIM
    #define RASTERON_ENABLE_FONT
    
    #include "Rasteron.h" // includes Rasteron main modules
#endif
#if ${Assimp_FOUND}
	#define ASSIMP_FOUND
#endif