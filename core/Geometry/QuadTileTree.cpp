#include "Geometry/QuadTileTree.hpp"

#include <cmath>
#include <cstdio>

void createQuadTree(Topl_SceneGraph* sceneGraph, unsigned divs){
    // static unsigned qTreeCount;
    // This is where you check for duplicates

    /* unsigned segCount = (unsigned)pow(4.0f, (double)divs - 1.0f);
    Geo_Rect2D* screenSegs = (Geo_Rect2D*)malloc(sizeof(Geo_Rect2D) * segCount);

    // All the creation code is here
    for(unsigned seg = 1; seg < segCount; seg *= 4){
        // Need a recursion for every seg, I have several identical tiles
        for(unsigned s = 0; s < seg; s++)
    } */

    //free(screenSegs);
    return;
}