#include "Geometry.hpp"

/* bool rectIntersectTest(const Rect* rect1, const Rect* rect2, const Eigen::Vector2d* r2_posFromZero){
    // r2_posFromZero
    return true;
}

bool circleIntersectTest(const Circle* circle1, const Circle* circle2){
    return true;
} */

// iterateDynamicNotes will likely be a helper function
void iterateDynamicNodes(std::vector<Topl_GeoEntity*>& dynNodes);